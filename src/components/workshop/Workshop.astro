<div class="workshop">
  <slot />
</div>

<style lang="scss" is:global>
  .workshop {
    border-block-end: 1px solid var(--color-yellow-400);
    padding-block-end: 1lh;
  }

  .workshop > * + * {
    margin-block-start: 1lh;
  }

  .workshop-people > * + * {
    margin-block-start: 0.25lh;
  }

  .workshop-people-list {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
  }
</style>
